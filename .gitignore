/target/
**/node_modules/
.DS_Store
*.log

# tokens/generated/ is COMMITTED — token-forge verify gate enforces parity.
# Do NOT add tokens/generated/ to .gitignore.

# React build artifacts (per-target, not the source)
adapters/react/dist/
adapters/react/.vite/

# Compose / Android
adapters/compose/build/
adapters/compose/.gradle/

# agent onboarding kept local-only (cruft standard)
AGENTS.md
